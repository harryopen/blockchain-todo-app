// Backup of TypeScript Hardhat config — moved aside to allow CommonJS config to be used.
import { defineConfig } from "hardhat/config";
import "@nomicfoundation/hardhat-toolbox";

export default defineConfig({
  solidity: {
    version: "0.8.28",
  },
});
